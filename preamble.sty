\ProvidesPackage{preamble}

% Use Vietnamese
% \usepackage[utf8]{vietnam}
% \usepackage[vietnamese=nohyphenation]{hyphsubst}
% \usepackage[vietnamese]{babel}

% Import file 
\usepackage{import}
\usepackage{subfiles}

% Image related package
\usepackage{graphicx}
\usepackage{float} 
\usepackage{caption}
\usepackage{subcaption}

% Layout page
\usepackage[margin=2.5cm]{geometry}
\usepackage{ragged2e}

% View layout with \layout
\usepackage{layout}

% Generate raw text
\usepackage{blindtext}

% Table format
\usepackage{multirow}
\usepackage{array}
\setlength{\tabcolsep}{10pt}
\renewcommand{\arraystretch}{1.5}
\usepackage{makecell}

% Allow redirect when click image reference
\usepackage[hidelinks]{hyperref}

% Reference document 
% sorting=ynt
% backend=biber,

\usepackage[
style=numeric,
]{biblatex}
\addbibresource{reference.bib}

% \usepackage{bibtex}

% Add code in document
\usepackage{listings}
\usepackage{color}

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\lstset{frame=tb,
  language=Java,
  aboveskip=3mm,
  belowskip=3mm,
  showstringspaces=false,
  columns=flexible,
  basicstyle={\small\ttfamily},
  numbers=none,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{blue},
  commentstyle=\color{dkgreen},
  stringstyle=\color{mauve},
  breaklines=true,
  breakatwhitespace=true,
  tabsize=3
}

% Number image according to section
\usepackage{chngcntr}
\counterwithin{figure}{section}
\usepackage{indentfirst}

% Paragraph itendent
\usepackage[skip=0.3cm]{parskip}

\usepackage{multirow}
\usepackage[table,xcdraw]{xcolor}